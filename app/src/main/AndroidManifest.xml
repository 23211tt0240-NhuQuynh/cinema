<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CinemaSgv"

        >

        <activity
            android:name=".MemberActivity"
            android:exported="true" />
        <activity
            android:name=".LichChieuActivity"
            android:exported="true" />

        <activity
            android:name=".LienHeUserActivity"
            android:exported="true" />
        <activity
            android:name=".TraLoiLienHeActivity"
            android:exported="true" />
        <activity
            android:name=".PhanHoiChoUserActivity"
            android:exported="true" />

        <activity
            android:name=".QuanLyPhimActivity"
            android:exported="true" />
        <activity
            android:name=".QuanLyLienHeActivity"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

    </application>

</manifest>