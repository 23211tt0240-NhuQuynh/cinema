<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền trắng -->
    <solid android:color="#FFFFFF" />

    <!-- Viền xám nhạt -->
    <stroke
        android:width="1dp"
        android:color="#DCD9DD" />

    <!-- Độ bo góc -->
    <corners android:radius="8dp" />
</shape>